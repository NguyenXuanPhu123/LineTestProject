import { DragDropCustomDirective } from './drag-drop-custom.directive';

describe('DragDropCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropCustomDirective();
    expect(directive).toBeTruthy();
  });
});
